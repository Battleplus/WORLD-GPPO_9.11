# M-10 联合任务集合后果开发 pilot 结果

日期：2026-09-14  
源码：分支 `joint-consequence-design-20260914`，启动前 HEAD `83f6d6342be9cc3a40b8832c60ef3ce99fa611ad`  
范围：开发 train/validation；没有 test/OOD、策略融合或通知实验。

## 运行与数据

使用 `world-gppo-911-local-pilot-venv`、Python 3.11.15、Torch 2.7.0+cu128、CUDA 12.8、RTX 3060 Laptop 6 GiB、4 个 CPU 线程、0 个数据 worker、seed 1101。系统 Python 的 CPU-only Torch 未使用。启动前 GPU 已占用约 1.4 GiB，未停止其他进程；训练后同样约 1.4 GiB，未记录可归因于本进程的峰值显存。

冻结命令为生成器 v4 配置：8/4 个父 episode、每父最多4个前缀、每前缀最多25个合法候选、每候选3次重复、horizon 6、base seed 193001。实际生成 600 train 分支、243 validation 分支，共 843 分支和 7,278 次模拟环境步；输出时间戳跨度约6秒，命令编排总耗时约16.3秒。13个前缀因没有公开 pending 任务集合而跳过，未按结果筛选或补齐。

v2 生成结果因适配器没有透传完整 provenance 被废弃；v3/v4保留但只有 v4 的协议字段与最终训练输入一致。v4 的 manifest、split SHA、身份、任务集合、Graph-5 快照、prefix trace 和外生 key 审计通过；隐藏/未来输入字段为0。

## 标签审计

| split | 记录 | 有效聚合标签 | 删失 | 父 episode | 父/前缀 | 父/前缀/重复组 | 可比较前缀 | 有非零候选均值差异 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| train | 600 | 383 | 217 | 8 | 25 | 75 | 21 | 13 |
| validation | 243 | 167 | 76 | 4 | 10 | 30 | 9 | 4 |

在可比较的父/前缀层面，train 有 12/20 个非零均值差异，validation 有 4/9 个。重复间非零范围的 action series 为 train 16/142、validation 2/59；因此存在有限机会成本信号，但三次重复不足以证明期望最优动作。大量候选结局相同的前缀被保留。

## 训练

训练入口：`tools/train_m10_joint_consequence_model.py`。实际完成4 epochs、152次 optimizer updates、13.14秒，停止原因 `completed_epochs`，未达到512 updates或20分钟上限。validation 有效记录固定为167。

| epoch | updates | validation total MSE | on-time MSE | deadline-failure MSE |
|---:|---:|---:|---:|---:|
| 0 | 38 | 0.283379 | 0.240934 | 0.325825 |
| 1 | 76 | 0.271760 | 0.249800 | 0.293720 |
| 2 | 114 | 0.250666 | 0.198275 | 0.303056 |
| 3 | 152 | 0.208720 | 0.178408 | 0.239032 |

best 推理 checkpoint 与 last recovery checkpoint 分离保存。last 含模型、optimizer、更新数、数据顺序、RNG 和早停状态；加载两者后预测有限且一致。当前没有额外中断续跑实测，不能把“支持 resume”写成已完成恢复实验。

## 基线与排序（开发证据）

下表按父 episode/前缀聚合重复结果，避免把重复分支当成独立父 episode。regret 是相对该组观察到的最佳有效候选的按时完成数差距；仅用于开发诊断。

| split | 方法 | 可比较前缀 | Top-1 命中率 | 平均 regret |
|---|---|---:|---:|---:|
| train | 当前规则参考动作 | 14 | 50.0% | 0.3571 |
| train | 公开联合机会成本评分 | 20 | 60.0% | 0.3000 |
| train | 零差值/固定最小动作 | 20 | 55.0% | 0.3500 |
| train | 联合后果模型 | 20 | 75.0% | 0.1167 |
| validation | 当前规则参考动作 | 7 | 57.1% | 0.3810 |
| validation | 公开联合机会成本评分 | 9 | 55.6% | 0.4074 |
| validation | 零差值/固定最小动作 | 9 | 55.6% | 0.4074 |
| validation | 联合后果模型 | 9 | 100.0% | 0.0000 |

模型 validation 总 MSE 为0.208720；按任务集合大小的 train 均值基线为0.276522。可是候选—参考配对差的 validation MSE 为0.145570，反而高于零差值基线0.129252。结合 validation 被用于早停、只有4个父 episode和9个可比较前缀，不能宣称额外排序价值或稳定调度增益；上述100%仅是开发信号。

## 结论与停止边界

1. 数据中存在有限、可重复检查的任务集合机会成本差异，但信号稀疏，重复数不足，尚未形成期望收益证明。
2. 公开联合基线在开发 validation 仅 5/9 个父/前缀组命中观察到的最佳候选，不能解释全部差异；但零差值基线同样为5/9，说明当前小样本上公开评分的额外解释力有限。
3. 轻量模型在绝对计数 MSE 和开发排序上出现信号，但配对差 MSE 未超过零差值基线；因此“预测有效”与“动作机会成本排序有效”均未通过正式门槛。
4. 本 pilot 支持的下一步最多是预先冻结的独立父 tape 验证；不支持正式策略融合、GPPO 增益结论或扩大训练预算。本轮训练和归档完成不等于研究假设通过。

通知机制、调度规则、奖励、事件触发和旧到达时间/系统能耗路线均未改变。历史通知收益仍是工程候选，不是世界模型贡献。

