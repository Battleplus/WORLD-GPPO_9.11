# M-10 到达协议系统可用性闭环

状态：已完成一次有限的、同 tape 配对的规则基线复现；本轮不训练、不调用世界模型、不连接原服务器。

## 1. 验收对象与证据边界

本轮把三个对象分开报告：

1. 软件训练/恢复链路：沿用此前已验证的后果模型训练与恢复证据；本轮没有新增训练或恢复运行。
2. 调度执行系统：在到达协议下，用同一组 16 条 test tape 分别运行无通信损伤和冻结弱通信条件，规则仅读取合法公开信息。
3. 世界模型增益：本轮不重跑预测评估；此前候选决策价值未建立，因此不作为系统基线的前置条件，也不把本轮规则结果写成世界模型收益。

旧持续服务协议、旧 Release 和旧账本继续保留。旧审计中的 `not_recorded` 仍是历史限制，不能被本次新 replay 反写成旧运行的原始记录。

## 2. 任务合同

协议为 `world-gppo-9.11-arrival/0.1.0`，研发主口径为 `arrival_to_region` + `physical_arrival`：物理进入指定区域即完成。物理到达时间、完成消息生成/发送、主机合法收到/确认时间、deadline 和观察截止时间分别记录。

`host_confirmation` 只作为独立通信敏感性指标；最终业务验收采用物理到达还是主机确认仍待确认。本轮理想条件是模拟器中的 `ideal` 通信 profile，不是实网验证；`composite` 是当前冻结弱通信 profile。两者使用同一批 tape、任务、规则和随机前缀，差异只归因于通信 profile 的模拟条件，不能外推为生产能力。

## 3. 配对复现

运行标识：`m10-system-closure-paired-20260914-v1`。16 条 tape 为 `test-mixed-seed-2093001` 至 `test-mixed-seed-2093016`，每条 6 个任务，共 96 个任务。完整逐步事件、逐任务结局、通信账本、执行账本、时钟账本和决策计时保存在外部运行目录：

`E:\Z博士\9.2日\WORLD-GPPO_9.11-local-runs\m10-system-closure-paired-20260914-v1\system-closure.json`

文件大小 44,413,064 bytes，SHA-256：`8a251c2a70992e4d26ddea1a57d76bab1175cc8d078e9f02effe7427d1f93b75`。

| 条件 | 物理到达事件 | deadline 前物理到达 | 主机确认事件 | deadline 前主机确认 | 任务结局覆盖 | 观察到的安全违规 |
|---|---:|---:|---:|---:|---:|---:|
| ideal（无通信损伤 profile） | 96 | 96/96（100%） | 96 | 95/96（98.96%） | 96/96 | 0 |
| composite（冻结弱通信 profile） | 48 | 48/96（50.00%） | 10 | 9/96（9.375%） | 96/96 | 0 |

ideal 中 1 个任务物理按时到达，但主机确认在 deadline 后到达；因此物理到达率与按时主机确认率不是同一指标。composite 的 `host_status` 为 9 个 `completed`、39 个物理到达但未按时确认、48 个未物理到达；新 replay 的 96 个任务均有明确终态或观察截止状态，`unresolved_rows=0`。这只是新运行证据，不是对旧运行缺失 ledger 的补写。

返回值均为原环境返回，ideal 为 59.516780，composite 为 16.498260；不把它们与旧协议完成率混合统计。

## 4. 48 个未按时物理到达任务的失效位置

以下是 composite 条件下 48 个物理到达失败任务的互斥主因统计。每个任务只计一个主因；其他可观察因素保留在逐任务 `failure_contributors` 中。主因分类是对当前账本的定位，不是最短路径可行性证明，也不意味着规则失败任务在其他调度器下必然不可行。

| 主因 | 任务数 | 解释 |
|---|---:|---|
| `scheduling_no_selection` | 20 | 已出现候选，但规则在观察窗口内没有形成该任务的实际分配尝试；属于调度选择链路，不等同于信息缺失。 |
| `execution_gate_or_resource` | 16 | 有尝试但受到执行门禁、资源占用/不可用、能量、租约或相关执行状态约束；这些拒绝/阻止不计为安全违规。 |
| `time_or_execution_budget` | 9 | 已进入执行或仍有执行状态，但在 deadline/观察截止前没有完成物理到达。 |
| `command_transport` | 3 | 形成命令但命令传输状态为丢弃/未到达，未把它重复计入调度或资源主因。 |
| 合计 | 48 | 互斥主因覆盖全部未按时到达任务。 |

当前统计未把任务强行归入 `information_or_telemetry` 或 `unknown`。移动、断联、能量、lease 和命令/执行事件仍逐任务保留；一个任务可能有多个贡献因素，但主因合计不重复计数。

## 5. 已物理到达但未按时主机确认

composite 中除 9 个 deadline 前主机确认外，有 39 个物理到达任务未满足按时主机确认。现有通信账本给出的原因为：

| 主机确认路径 | 任务数 | 解释 |
|---|---:|---|
| `completion_notice_dropped_or_not_generated` | 26 | 没有可关联的完成消息记录，当前证据不能进一步把“未生成”和“发送后丢弃”混为一个更细类别。 |
| `not_legally_received_by_observation_cutoff` | 7 | 有发送记录，但在观察截止前没有合法收到记录。 |
| `received_after_deadline` | 1 | 消息在 deadline 后收到；不计为按时确认。 |
| `received_but_duplicate_or_identity_rejected` | 5 | 关联消息走到 stale/duplicate 或身份拒绝路径；这是拒绝路径，不是已接受的重复/越权执行。 |
| 合计 | 39 | 覆盖全部物理到达但未按时确认任务。 |

本轮没有预先假定需要重传，也没有关闭过期、身份或 fencing 校验。ideal 条件仍有 1 个“收到但晚于 deadline”案例，说明主机确认口径必须单独报告。

通信代理计数必须按链路和状态解释，不能当作真实网络流量。composite 原始账本计数为：ACK received 204、dropped 14；command sent 390、received 148、dropped 25；telemetry sent 11,145、received 9,913、stale_or_duplicate 5。不同链路的发送、接收、丢弃和过期不是同一分母，不能相减拼成实网吞吐量。

## 6. 安全与执行证据

两种条件均记录 `observed_safety_violations=0`、`unauthorized_or_fenced_accept=0`、`guard_rejections=0`。检查范围覆盖重复接受、越权接受、错误 ACK/身份、失效 lease 继续执行和 fencing 绕过的执行账本路径；正常 stale、过期、资源不足拒绝不算违规。

该结论只能写成：“本次 replay 在已记录审计范围内未观察到违规。”它不能替代旧训练 raw safety ledger、真实网络验证、生产保证或弱通信可用性证明。真实网络流量本轮没有实测。

## 7. 本机计算计时

计时范围是规则决策调用返回的本地 wall time，包含本次决策所需的模拟计算，但不包含结果文件落盘；线程配置和完整硬件计时没有被用来宣称实时性能。ideal 记录 162 个决策样本，mean 2.745009 ms、P95 4.064845 ms、max 45.715100 ms；composite 记录 256 个样本，mean 2.023146 ms、P95 3.417775 ms、max 8.436300 ms。两者的样本数和控制流不同，不能将均值直接解释为通信造成的加速或减速。

## 8. 当前可用范围与后续边界

- 功能跑通：到达协议下的规则调度、物理到达与主机确认分离记录、故障/通信/执行账本和有限配对条件已跑通。
- 条件实测能力：在本次 16 tape 仿真中，ideal profile 为 96/96 按时物理到达，composite profile 为 48/96；按时主机确认分别为 95/96 与 9/96。
- 世界模型增益：本轮未验证，旧后果模型候选决策价值仍未建立；暂停新增世界模型训练，不以它作为规则系统闭环的前置条件。
- 生产/竞赛可用性：未宣称通过。业务门槛、真实网络流量和更广泛分布下的系统能力仍需明确/验证。

后续是否继续算法研究，应先看失效是否主要发生在信息和资源链路已经允许完成、而调度选择仍有可改善差距。若主要瓶颈是通信或资源超出当前合同能力，应先补齐可观测失效边界和需求，而不是自动追加世界模型训练。

