# 会议一页说明：到达协议 GPPO 融合选型

**证据：** 64 个共享测试父 tape；3 个训练 seed；每学习组每 seed 8192 环境步。  
**主口径：** 按时物理到达；主机确认独立统计。  
**状态：** 训练链路完成，研究收益未建立，工程默认不采用融合。

## 五句话结论

1. 冻结世界模型已真实进入融合 GPPO 的采样分布和 PPO log-prob 重算，端到端融合训练完成。
2. 融合按时物理到达 `542/1152`，History `540/1152`，只多 2 个任务；seed 差值为 `-11/+9/+4`。
3. 融合低于 GPPO `565/1152`，也低于一次共享规则参考 `196/384`（51.04%）。
4. 融合完整决策均值 `10.6660 ms`，History `6.2347 ms`，高约 71.1%；当前收益不足以证明值得成本。
5. 选型：规则为工程参考，GPPO 为学习基线，融合保留为研究制品，不设为默认；这不是对世界模型普遍无效的证明。

## 口径边界

- tape bootstrap 的单位是“每个 6 任务父 tape 的任务差”，只覆盖测试 tape 不确定性，不覆盖训练 seed 不确定性。
- 规则只有一次共享评估，不能复制成三个独立 seed。
- 安全违规为 0 仅限本轮记录的审计范围，不是生产安全保证。
- 训练 raw rollout ledger 未单独导出；测试 ledger 未被用来冒充补齐。
- 旧持续服务协议、通知工程收益和旧负结果不与本轮混合。

## 后续决策

本轮不新增训练、不扩样、不改模型。若未来继续，只能先登记新的业务成本门槛、策略预算和训练 rollout ledger 导出要求，再单独预注册验证；不自动执行。
